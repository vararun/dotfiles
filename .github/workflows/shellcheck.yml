name: ShellCheck

on:
  push:
    paths:
      - "scripts/bootstrap.sh"
      - ".github/workflows/shellcheck.yml"
  pull_request:
    paths:
      - "scripts/bootstrap.sh"
      - ".github/workflows/shellcheck.yml"

jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run ShellCheck
        uses: ludeeus/action-shellcheck@2.0.0
        env:
          SHELLCHECK_OPTS: -s bash -e SC1071
        with:
          scandir: "./scripts/bootstrap.sh"
          severity: "error"
